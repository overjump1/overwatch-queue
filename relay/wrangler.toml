name = "overwatch-queue-push-relay"
main = "src/worker.ts"
compatibility_date = "2024-11-01"

# Non-secret config. Set with `wrangler deploy` after filling these in, or override per
# environment with `[env.<name>.vars]` blocks — see README.md.
[vars]
APNS_TEAM_ID = "56N4LCCUN2"
APNS_KEY_ID = "4WRV6676P3"
APNS_BUNDLE_ID_IOS = "com.tomerady.OverwatchQueue"
APNS_BUNDLE_ID_WATCH = "com.tomerady.OverwatchQueue.watchkitapp"

# Secrets are NOT set here — never commit a private key or API key to this file.
# Set them once with:
#   wrangler secret put APNS_PRIVATE_KEY
#   wrangler secret put RELAY_API_KEY
