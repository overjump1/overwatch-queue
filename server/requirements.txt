PyQt6>=6.4
# The live transport to the phone/watch: connects to a local Mosquitto broker (started
# and managed by owqserver.mqttbroker) rather than talking raw sockets itself.
paho-mqtt>=2.0
# Optional: only needed to push Live Activities, which go through Firebase Cloud
# Messaging rather than straight at Apple — see owqserver/fcm.py for why. Without a
# service account configured the server simply sends no pushes.
google-auth>=2.28
requests>=2.31

# Optional: only needed to read the hero-select screen and click heroes in-game. Without
# them hero select is driven by hand from the panel, exactly as it always was — which is
# also the only option on macOS, where `pygetwindow` can't focus the game.
opencv-python>=4.8
numpy>=1.24
mss>=9.0
pyautogui>=0.9.54
pygetwindow>=0.0.9

# Optional: only needed to read which real maps are on the vote screen. Without it,
# map vote falls back to plausible-but-not-real catalog options, exactly as it always
# did — a real OCR model, not a system binary to install alongside it the way
# `pytesseract` would need, but a meaningfully larger download than anything else here.
#
# 1.2.3 is the floor, not 1.3: the 1.3+ releases publish no Python 3.13 build, so pinning
# them made `pip install -r` fail outright there — before it reached anything after this
# line. 1.2.3 has the same `(result, elapse)` call shape this uses, and
# pip still picks the newest release a given Python actually has.
rapidocr-onnxruntime>=1.2.3

# Optional, Windows only: lets owqserver.bnetpresence read player state from Battle.net
# itself over its embedded CEF debugging port (see that module's own docstring, and
# run.py's --presence-source flag). Without it, presence falls back to reading the same
# record straight out of Battle.net's own process memory, which needs no extra install
# at all.
websocket-client>=1.7
