<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Installing the APK the app downloads from its GitHub release. The user still has to allow
         it for this app, and still confirms each install. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".OwqApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="OverQueue"
        android:supportsRtl="true"
        android:theme="@style/Theme.OverQueue">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Scanning the PC's QR code with the camera app opens overqueue://pair?id=... here.
                 owq:// is the same link under the name the app used to go by, and a PC that
                 hasn't been updated yet still writes that one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="overqueue" android:host="pair" />
                <data android:scheme="owq" android:host="pair" />
            </intent-filter>
        </activity>

        <service
            android:name=".QueueMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- How the system installer reports back on an update (see UpdateInstallReceiver). -->
        <receiver
            android:name=".UpdateInstallReceiver"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="status" />
    </application>

</manifest>
